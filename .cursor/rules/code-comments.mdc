---
description: 关键代码注释规范——写什么、不写什么，供后续 Agent 执行
alwaysApply: true
---

# 关键代码注释规范

新增或修改功能时，必须在**关键逻辑**处补充简洁注释；不要为显而易见的代码写注释。

## 必须注释

- 模块 / 文件职责（文件顶部或 `<script setup>` 顶部 1–3 行）
- 非直觉行为、约束、陷阱（如 KeepAlive 的 `name` 必须与路由 `name` 一致）
- 状态机 / 缓存 / 刷新等「为什么这样做」（如动态 `key`、持久化、路由守卫）
- 公共 API、`meta` 字段、跨包约定的含义
- 临时方案 / 演示逻辑（标明后续替换点）

## 不要注释

- 复述代码字面意思（如 `// 设置 loading 为 true`）
- 对每个 import、简单赋值、常规 CRUD 逐行说明
- 大段教程式说明或过时注释

## 写法要求

- 中文；先写「做什么 / 为什么」，少写「怎么做」
- 单行优先；复杂处用短块注释
- 注释与代码同步更新，删代码时删对应注释

## 示例

```ts
// ❌ BAD
// 把 refreshKeys 加 1
refreshKeys.value[path] = (refreshKeys.value[path] ?? 0) + 1

// ✅ GOOD
// 递增 key，强制 KeepAlive 子组件重建；平时切换标签复用旧值以保留缓存
refreshKeys.value[path] = (refreshKeys.value[path] ?? 0) + 1
```

```vue
<!-- ❌ BAD：组件内每个按钮都写「点击关闭」 -->
<!-- ✅ GOOD：文件头说明职责 + 关键分支注释 -->
<script setup lang="ts">
/**
 * 多标签栏：打开同步、切换、关闭、右键菜单、刷新。
 */
// 关掉当前页时，跳到剩余标签的最后一个
</script>
```

## Agent 执行清单

写完功能后自检：

1. 新文件是否有职责说明？
2. 守卫 / 缓存 / 鉴权 / 跨包约定是否标注？
3. 是否存在可删的废话注释？
